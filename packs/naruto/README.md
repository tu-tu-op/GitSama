# Naruto Voice Pack

The default bundled GitSama voice pack for local Git events.

The repository maintainer has confirmed permission to redistribute the included audio files with GitSama.

This pack maps the v1 GitSama events in `pack.toml` to MP3 files under `audio/`.
